{
  "name": "aimen-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://altaiyeb.info",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.js",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.1",
    "gh-pages": "^5.0.0",
    "sass": "^1.63.6",
    "vite": "^4.4.0"
  }
}
